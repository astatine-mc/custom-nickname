# 보안·구조·UX 검토 및 수정 결과

## 범위와 판단 기준

Proxy와 lobby 최신 로그를 먼저 확인하고 닉네임 플러그인 소스를 정적으로 검토했다. 로그의 DB 인증 실패는 자격 증명이 필요한 운영 문제이며 이번 코드 변경으로 비밀번호가 생성되거나 인증이 해결되지는 않는다. 아래 항목은 코드에서 확인한 결함과 회귀 테스트 범위를 구분한 기록이다.

## 예상 질문과 대응

| 관점 | 예상 질문 | 확인한 문제와 이번 처리 |
|---|---|---|
| 개발자 | DB 초기화에 실패하면 접속이 허용되는가? | names가 null일 때 로그인 검사를 건너뛰었다. 준비 완료 플래그를 도입해 준비 실패·종료 중 접속을 거절하고 backend 이벤트를 건너뛴다. 초기화 실패 시 풀도 닫는다. |
| 개발자 | backend가 요청하지 않은 변경 또는 같은 응답을 반복해 보내면? | 출처·UUID만 확인하고 TICKET_USE를 받았다. PendingChanges가 플레이어·requestId·이름·동일 서버 연결을 묶고 15초 이내 한 번만 소비한다. 서버 이동·종료 시 제거한다. |
| 코드 설계자 | 비동기 응답이 역순으로 도착하면 최신 이름이 과거로 되돌아가는가? | 캐시만 보호하고 오래된 DTO를 콜백에 전달했다. ProfileCache로 버전 비교를 분리하고 최신 값을 반환·알림한다. 전송 직전에도 현재 캐시를 읽는다. 이미 실행 중인 외부 TAB 작업까지 원자적으로 직렬화한 구조는 아니다. |
| UX 디자이너 | 친구의 변경권을 실수로 사용하면 사라지는가? | 타인 소유를 UNKNOWN으로 분류해 삭제했다. ISSUED 상태 거절로 변경하여 사용은 차단하고 원래 소유자에게 반환하도록 안내한다. |
| UX 디자이너 | DB 저장은 성공했는데 표시 콜백이 실패하면 변경 실패로 보이는가? | 알림 예외가 성공 future를 실패시킬 수 있었다. commit 이후 알림 실패는 기록하고 저장 결과는 유지한다. 성공 안내 전 프로필 반영을 먼저 시도한다. |
| 개발자 | 예상하지 못한 예외가 사용자에게 내부 정보를 노출하는가? | 모든 getMessage를 반환했다. 명시적 입력 오류·DB 예외만 안내하고 나머지는 일반 오류 문구로 감춘다. 변경 실패의 내부 원인은 서버 로그에 남긴다. |

## 요청 제한의 정확한 범위

PendingChanges는 요청하지 않은 응답과 동일 응답의 재전송을 차단하는 상관관계 검사다. 인증된 backend 자체가 침해된 경우 손 아이템 보유 증명을 대신하지는 않는다. backend 접근은 프록시 전달 설정과 네트워크 접근 제한으로 보호해야 한다.

플레이어별 미완료 손 아이템 확인 요청은 하나다. 손에 변경권이 없어 응답이 오지 않으면 15초 후 다시 요청할 수 있다. DB 처리 전체에 대한 초당 요청 제한이나 전역 plugin message rate limit은 이번 범위에서 구현하지 않았다.

## 남은 문제

- 발급 후 전달 ACK와 복구 지급이 없다. 인벤토리가 가득 차면 발급 행만 남을 수 있고 관리자 지급 성공 안내는 최종 아이템 수신을 보장하지 않는다.
- DB requestId 재조회는 토큰·이름·작업 종류를 완전히 바인딩하지 않는다. 이번 네트워크 1회 요청 검사는 공개 서비스 API를 직접 호출하는 신뢰된 플러그인의 멱등성 문제까지 해결하지 않는다.
- 원문 placeholder 노출과 CustomNameplates 반사 API 호환은 실제 서버 reload 검증이 필요하다.
- 캐시 TTL과 외부 쓰기 동기화, 다중 Velocity 무효화는 없다.
- 고유 ID는 bearer token이다. 운영 로그에 아이템 토큰·비밀번호를 출력하지 않아야 한다.

## 검증

추가 회귀 테스트는 잘못된 이름·연결·미발행 요청·재전송 거절, 요청 만료·정리, 낮은 revision 거절, ISSUED/USED 제거 정책을 검증한다. 실제 MariaDB 트랜잭션 동시성, 서버 초기화 실패 이벤트, 외부 TAB 렌더링은 단위 테스트 범위에 포함되지 않는다. 서버를 재시작하지 않았으므로 런타임 검증 완료로 간주하지 않는다.
