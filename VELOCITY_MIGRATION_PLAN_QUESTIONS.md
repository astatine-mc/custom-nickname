# Velocity 닉네임 전환안 검토 질문

개발자와 UX 디자이너가 [Velocity 전환안](./VELOCITY_MIGRATION_PLAN.md)을 검토할 때 먼저 확인해야 할 질문 5가지입니다.

## 1. 닉네임 변경 후 화면 갱신이 늦어지면 어떤 순서로 복구할 것인가?

**대상: 개발자 · UX 디자이너**

MariaDB 저장을 완료한 뒤 화면은 다음 우선순위로 갱신합니다.

1. **1순위: TAB tab list** — 프록시에서 즉시 최신 닉네임을 반영합니다. 다른 서버에 있는 플레이어에게도 먼저 전파합니다.
2. **2순위: CustomNameplates** — 현재 월드의 머리 위 이름을 갱신합니다.
3. **3순위: Paper display name과 채팅** — 채팅, 메시지, 이벤트에 사용하는 표시 이름을 갱신합니다.
4. **4순위: HUD·scoreboard·기타 연동 플러그인** — `NicknameUpdatedEvent`를 통해 순차적으로 갱신합니다.
5. **5순위: 검색·관리자 화면의 캐시** — 최신 revision을 기준으로 비동기 갱신합니다.

TAB과 CustomNameplates가 잠시 다른 이름을 표시할 수 있으므로, 사용자에게 보여주는 성공 메시지는 DB commit 완료가 아니라 1순위 TAB 갱신 성공을 기준으로 할지 정해야 합니다. 나머지 표시가 늦어질 때는 닉네임 변경을 실패로 표시하지 않고 revision 기반 재적용 작업으로 복구해야 합니다.

## 2. 변경권 사용 중 서버 이동이나 연결 끊김이 발생해 결과가 불명확하면 어떻게 복구하는가?

**대상: 개발자**

변경권은 아이템에 들어 있는 bearer token이므로, 아이템의 화면 상태와 DB의 사용 상태가 잠시 달라질 수 있습니다. 특히 다음 네 가지 시점이 서로 다른 결과를 만들 수 있습니다.

1. 요청이 Velocity에 도착하기 전 플레이어가 서버를 이동하거나 연결을 끊습니다.
2. MariaDB commit 전에 연결이 끊깁니다.
3. MariaDB commit은 성공했지만 Velocity 응답이 Paper에 도착하지 않습니다.
4. 성공 응답은 도착했지만 아이템 제거 전에 Paper 서버가 종료됩니다.

### 변경권 고유 ID 정책

모든 변경권 아이템에는 `customnickname:ticket_id` PDC 값으로 변경권 고유 UUID를 저장합니다. 아이템 이름, lore, material은 신뢰하지 않고 이 고유 ID만 서버에 전달합니다.

Velocity는 고유 ID를 해시해 `cn_nickname_tickets.token_hash`의 상태를 확인합니다.

- `ISSUED`: 현재 플레이어가 사용할 수 있습니다. 닉네임 검증과 DB 트랜잭션이 성공한 뒤 `USED`로 변경합니다.
- `USED`: 이미 사용된 변경권입니다. 닉네임 변경을 실행하지 않고, 해당 ID를 가진 아이템을 플레이어 인벤토리에서 제거합니다.
- `EXPIRED` 또는 `CANCELLED`: 사용할 수 없는 변경권입니다. 동일하게 해당 ID의 아이템을 제거합니다.
- DB에 등록되지 않은 ID: 위조·손상·잘못된 복제 아이템으로 보고 사용할 수 없게 하며, 해당 ID의 아이템을 제거합니다.

동일한 고유 ID를 가진 복제 아이템이 여러 장 있으면 하나가 먼저 `USED`가 되는 순간 나머지도 모두 무효입니다. Paper 브리지는 `NICKNAME_RESULT`의 `REMOVE_TICKET_ID` 지시를 받으면 현재 플레이어의 인벤토리, off-hand, cursor에서 해당 ID를 가진 모든 아이템을 제거합니다. 제거 후에는 플레이어에게 “이미 사용된 변경권을 제거했습니다” 또는 “사용할 수 없는 변경권을 제거했습니다”라고 안내합니다.

재접속, 서버 이동, Paper 브리지 ready, TAB·CustomNameplates reload와 같이 인벤토리 복구가 필요한 시점에는 Paper가 플레이어 인벤토리의 ticket ID 목록을 Velocity에 보냅니다. Velocity는 DB에서 각 ID의 상태를 확인하고 `ISSUED`가 아닌 ID 목록을 돌려줍니다. Paper는 응답받은 ID에 해당하는 모든 아이템을 즉시 제거합니다. 이 검사는 닉네임 변경 이력이나 티켓 사용 상태를 되돌리지 않으며, 사용된 티켓을 다시 발급하지 않습니다.

권장 기본 정책은 `ISSUED → USED` 두 상태만 원자적으로 전환하는 것입니다. 별도의 `PROCESSING` 상태를 오래 남기면 프록시 강제 종료 뒤 운영자가 토큰을 수동 복구해야 하는 모호한 상태가 생기므로, DB 트랜잭션 안에서 다음 작업을 한 번에 처리합니다.

- `token_hash`와 `status = 'ISSUED'`를 조건으로 티켓을 잠급니다.
- 플레이어 UUID, 새 닉네임, `request_id`를 검증합니다.
- 프로필, 닉네임 이력, 감사 로그를 기록합니다.
- 티켓을 `USED`로 바꾸고 `used_by_uuid`, `used_at`을 기록합니다.
- 모든 작업을 한 번에 commit합니다.

commit이 성공한 순간부터는 닉네임 변경과 티켓 사용이 확정입니다. 응답이 유실되어도 같은 `request_id`로 재시도하면 동일한 성공 결과를 반환하고, 새로운 요청 ID로 같은 토큰을 보내면 `USED`로 거절합니다. 따라서 재시도가 변경권을 두 번 소모하거나 닉네임 이력을 중복 생성하지 않아야 합니다.

아이템 제거는 commit 이후 성공 응답을 받은 Paper 브리지에서 수행합니다. 응답 유실 또는 Paper 종료로 아이템이 남아도 토큰은 이미 `USED`이므로 재사용할 수 없습니다. 플레이어가 다시 접속하면 Paper 브리지가 보유 아이템의 토큰 상태를 Velocity에 조회하고, `ISSUED`가 아닌 고유 ID의 아이템은 관리자 판단을 기다리지 않고 제거합니다. 서버 이동 중 요청이 들어오면 Velocity는 연결 UUID와 현재 서버를 확인하고, 아이템 제거 명령은 현재 접속 서버에만 보냅니다.

시간 제한과 사용자 경험도 함께 정해야 합니다.

- 응답 대기 중에는 플레이어에게 “변경 처리 확인 중”을 표시하고, 타임아웃을 곧바로 “변경 실패”로 안내하지 않습니다.
- 재접속 후 프로필과 티켓 상태를 대조해 “변경 완료” 또는 “변경권 미사용” 중 하나로 최종 안내합니다.
- 같은 요청을 다시 보낼 때는 `request_id`를 유지합니다.
- 운영자 복구 명령은 새 티켓을 임의로 만들지 않고 `ticket_id`, `request_id`, `audit_id`를 확인한 뒤 미전달 아이템만 재지급합니다.
- 이미 `USED`인 토큰은 복구 지급 대상이 될 수 없습니다.

검토 시에는 “토큰 사용 확정 시점”, “응답 유실 후 재접속 화면”, “아이템 제거 실패 처리”, “서버 이동 중 요청의 소유 서버”, “관리자 복구 권한”을 하나의 상태 전이 표로 확정해야 합니다. 이 정책이 정해져야 변경권을 안전하게 운영하면서도 플레이어가 같은 변경권을 잃었다고 느끼지 않게 할 수 있습니다.

## 3. 커스텀 닉네임과 실제 계정명을 사용자가 어디에서 구분해야 하는가?

**대상: UX 디자이너 · 개발자**

귓속말, 친구 추가, 관리자 조회에서는 표시 닉네임과 실제 계정명이 서로 다른 사람을 가리키지 않도록 해야 합니다. `닉네임`, `@계정명`, UUID를 언제 노출할지와 검색 결과에 어떤 보조 정보를 표시할지 정해야 합니다.

## 4. 한글 닉네임의 길이·금지어·유사 문자를 어떤 기준으로 처리할 것인가?

**대상: UX 디자이너 · 개발자**

한글 조합, 대소문자, 유니코드 정규화, 숫자·밑줄, 금지어, 비슷하게 보이는 문자에 대한 정책이 필요합니다. 입력 단계의 안내 문구와 최종 거절 메시지가 같은 규칙을 사용해야 하며, 이미 사용 중인 닉네임을 구분하기 쉬운 오류 메시지도 필요합니다.

## 5. TAB과 CustomNameplates reload 중 placeholder 원문 노출을 어떻게 막을 것인가?

**대상: 개발자 · UX 디자이너**

플러그인 reload 순간에는 `%customnickname_nickname%` 같은 placeholder가 잠시 원문으로 보이거나 표시 값이 비어 보일 수 있습니다. 이때 갱신 빈도를 높이면 reload 중인 플러그인이 불완전한 값을 더 자주 전파할 수 있으므로, TAB과 CustomNameplates의 갱신 주기를 평상시보다 낮게 설정하고 마지막 정상 표시 값을 유지하는 방식으로 우회합니다.

권장 정책은 다음과 같습니다.

- 평상시에도 TAB의 커스텀 닉네임 placeholder 갱신 주기를 낮은 빈도(예: 1초 이상)로 설정합니다.
- 평상시에도 CustomNameplates의 닉네임 갱신 주기를 낮은 빈도로 설정하고, reload 중에는 마지막 정상 이름을 유지합니다.
- reload 시작 시 placeholder 값을 비우거나 `%customnickname_nickname%` 원문으로 초기화하지 않습니다.
- reload 완료 신호를 받은 뒤 한 번만 최신 프로필을 재적용하고, 이후에도 낮은 빈도의 일반 갱신 주기를 유지합니다.
- Velocity와 Paper 브리지는 마지막 `NicknameProfile`을 캐시하고, 캐시가 없을 때는 placeholder 원문 대신 실제 계정명을 반환합니다.
- reload 중 잠깐 이전 닉네임이 보이는 것은 허용하되 placeholder 원문, 빈 이름, `null`은 사용자 화면에 노출하지 않습니다.

검토할 결정은 TAB과 CustomNameplates 각각의 최소 안정 갱신 주기, reload 완료 신호를 감지하는 방법, 마지막 정상 값의 보관 위치입니다. 목표는 reload 중 최신 이름을 매 순간 다시 계산하는 것이 아니라, 정상적인 이름을 계속 보여주고 reload 완료 후 한 번만 새 값을 적용하는 것입니다.
